# 语法分析及目标代码生成
在此前的词法及语法分析的基础上，我们已经得到了AST，接下来考虑通过两遍扫描完成：
- 第一遍扫描：构建符号表
    - 符号表用于存储设备，变量，规则等符号类型及属性，作为后续语义分析的基础，供查询验证标识符
    - symbol.h
        - 创建符号表数据结构定义
    - symbol.c
        - 实现symbol.h中定义的符号表操作函数
        - 实现AST遍历和符号表构建函数
    - parser.y
        - main中添加符号表构建
- 第二遍扫描：语义分析（检查）
    - 根据AST和符号表，检查变量类型是否正确，作用域规则，变量使用前是否声明，各动作规则是否合法等等，并进行相应的报错输出
    - semantic.h
        - 语义分析模块头文件
    - semantic.c
        - 实现语义分析模块
    - parser.y
        - main中添加语义分析
- 第三遍扫描：目标代码生成
    - 实现一个代码生成器，（需要前面保证合法）直接根据节点类型生成Python代码，各项动作函数的实现（比如开灯），我想的是正常翻译成一个Python函数，我们另外定义一个动作库homelang_rt.py，翻译的时候在import部分直接把动作库导入进来，然后翻译出来的代码直接通过调用动作库中相应的函数就可以实现打印这样一句话："灯已打开"
    - codegen.h
        - 代码生成器头文件
    - codegen.c
        - 实现代码生成器
    - parser.y
        - main中添加代码生成