# 1. 设备声明
device ELECTRIC_FAN parlour_electric_fan as "客厅电风扇"
device LIGHT corridor_light as "走廊灯"

# 2. 变量声明
var int counter;

# 3. 变量赋值
counter = 0;

# 4. 规则定义
rule loop_test {
    # 测试 while 循环
    while (counter < 3) do {
        set corridor_light.status to on;
        set corridor_light.status to off;
        counter = counter + 1;
    }

    # 测试整数范围 for 循环
       set parlour_electric_fan.status to on;
    for (i in [1, 5]) {
        set parlour_electric_fan.speed to i;
    }

    # 测试时间点 (HomeLang 特色)
    if (time between 22:00 and 23:00) {
        set parlour_electric_fan.oscillation to 0;
    }
}